name: tests

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  native-tests:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v5
      - name: Test
        run: swift test
      - name: Build release
        run: swift build -c release
